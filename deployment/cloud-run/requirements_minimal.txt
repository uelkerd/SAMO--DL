# Minimal Working Requirements - Extends Core Requirements
# Avoids PyTorch/safetensors compatibility issues

# Include core requirements
-r ../requirements-core.txt

# Minimal-specific overrides (if any needed)
# flask==3.1.1  # Override from core if specific version needed
# torch>=2.6.0,<2.7.0  # Override from core if specific version needed

# Note: This file now inherits all core dependencies from requirements-core.txt
# Add only minimal-specific dependencies or version overrides here 