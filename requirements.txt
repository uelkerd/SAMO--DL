############################################
# SAMO-DL Default Requirements File       #
# Lightweight install (no heavy ML deps)  #
############################################

# Core API dependencies (lightweight, no ML)
-r requirements-api.txt

# Development dependencies (testing, linting, etc.)
-r requirements-dev.txt

# Note: For ML functionality, install separately:
# pip install -r requirements-ml.txt
# Or use extras: pip install .[ml] 