# Test Utilities Dependencies
# These packages are required for the test utility functions to work correctly

# Core scientific computing (for audio file creation)
numpy>=1.19.0

# Data manipulation (for training data validation)
pandas>=1.3.0

# Deep learning framework (for GPU info utilities)
torch>=1.9.0

# Testing framework
pytest>=6.0.0

# Note: These dependencies are already included in the main project requirements
# This file serves as documentation for what's needed for the test utilities
